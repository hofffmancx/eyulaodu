<nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="/">俄语互联网资源精选</a>
            <div class="info-web">搜索<span style="font-style:oblique">&nbsp;&nbsp;好玩、有趣、有用&nbsp;&nbsp;</span>的互联网俄语资料</div>        </div>


        <!-- 搜索框 -->

        <div class="nav navbar-nav search-navbar">
          <%= form_tag search_groups_path , :method => :get do %>
           <div class="input-group" id="search-id" >
              <input type="text" class="form-control" name="q" value="<%= params[:q] %>" placeholder="搜索文章">
                <div class="input-group-btn">
                   <button class="btn  btn-default" type="submit">
                      <i class="glyphicon glyphicon-search"></i>
                   </button>
                </div>
            </div>
           <% end %>
        </div>

        <!-- 搜索框 完毕-->

        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav navbar-right">
              <% if !current_user %>
                  <li><%= link_to("注册", new_user_registration_path) %> </li>
                  <li><%= link_to("登录", new_user_session_path) %></li>
                <% else %>
                  <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                        Hi!, <%= current_user.email %>
                        <b class="caret"></b>
                    </a>
                    <ul class="dropdown-menu">
                        <li> <%= link_to("退出", destroy_user_session_path, method: :delete) %> </li>
                    </ul>
                  </li>
                <% end %>
                </li>
            </ul>
        </div>
    </div>

</nav>
<div >
  <% if current_user %>
      <%= link_to("Categories", categories_path)%>
  <% end %>
  <div class="index-head-1">
    <li class="category-index"><%= link_to("主页", groups_path) %>|</li>
    <% Category.all.each do |category| %>
      <li class="category-index" >
       <%= link_to(category.name, groups_path(category: category.name)) %> |
     </li>
    <% end %>
    <li class="category-index"><%= link_to("关于我", aboutme_index_path) %></li>
  </div>

</div>
